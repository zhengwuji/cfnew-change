# CFnew-change - Cloudflare Workers 代理订阅系统

<div align="center">

**一个基于 Cloudflare Workers 的自动化代理订阅管理系统**

[![GitHub Actions](https://img.shields.io/badge/GitHub-Actions-2088FF?logo=github-actions)](https://github.com/features/actions)
[![Cloudflare](https://img.shields.io/badge/Cloudflare-Workers-F38020?logo=cloudflare)](https://workers.cloudflare.com/)
[![License](https://img.shields.io/badge/License-MIT-green)](LICENSE)

</div>

---

## 📖 项目简介

CFnew-change 是一个完整的 Cloudflare Workers 代理订阅系统，具有以下核心功能：

- 🔐 **代码混淆保护** - 自动将明文源码混淆后部署
- 🚀 **自动化部署** - GitHub Actions 自动化工作流
- 🌐 **多客户端支持** - 支持 10+ 主流代理客户端
- 🎯 **智能优选** - 支持多种优选 IP/域名策略
- ⚡ **高性能** - 基于 Cloudflare 全球网络
- 🛠️ **简洁易用** - 图形化订阅管理界面

---

## 🎯 核心功能

### 1. 代码混淆与保护

本项目采用双层文件设计：

- **`明文源吗`** - 未混淆的源代码文件（方便开发和修改）
- **`少年你相信光吗`** - 混淆后的代码文件（用于实际部署）
- **`snippets`** - Cloudflare Workers Snippets 专用版本

当你修改 `明文源吗` 文件并推送到 GitHub 时，会自动触发混淆流程，生成 `少年你相信光吗` 文件。

### 2. 自动化部署

项目包含两个 GitHub Actions 工作流：

#### 工作流 1：代码混淆（obfuscate.yml）
- **触发条件**：推送修改 `明文源吗` 文件
- **执行流程**：
  1. 使用 `javascript-obfuscator` 混淆代码
  2. 生成 `少年你相信光吗` 文件
  3. 自动提交并推送到仓库

#### 工作流 2：发布部署（test.yml）
- **触发条件**：创建新标签（Tag）
- **执行流程**：
  1. 将 `少年你相信光吗` 重命名为 `_worker.js`
  2. 打包成 `Pages.zip`
  3. 创建 GitHub Release

### 3. 多客户端订阅支持

支持以下客户端自动转换：
- CLASH
- SURGE
- SING-BOX
- LOON
- QUANTUMULT X
- V2RAY
- Shadowrocket
- V2RAYNG
- NEKORAY
- STASH

### 4. 智能优选策略

- **原生地址** - 直接使用 Worker 域名
- **域名优选** - 预设的优质域名列表
- **GitHub 优选** - 从 GitHub 自动获取优选 IP 列表

---

## 🚀 快速开始

### 前置要求

- GitHub 账号
- Cloudflare 账号
- Git 环境（已配置好认证）

### 部署步骤

#### 第一步：Fork 或克隆仓库

```bash
git clone https://github.com/zhengwuji/cfnew-change.git
cd cfnew-change
```

#### 第二步：修改配置

编辑 `明文源吗` 文件（或 `snippets` 文件）中的核心配置：

```javascript
// --- 硬编码配置 ---
const authToken = '你的UUID';  // 替换成你自己的 UUID
let fallbackAddress = '';      // 可选：备用地址
let fallbackPort = '443';      // 可选：备用端口
const socks5Config = '';       // 可选：SOCKS5 代理配置
```

**重要配置说明：**

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `authToken` | 访问订阅的 UUID（必填） | `f64bdc57-0f54-4705-bf75-cfd646d98c06` |
| `customPath` | 自定义访问路径（可选） | `mypath` 或 `path/to/sub` |
| `manualWorkerRegion` | 手动指定地区（可选） | `US`, `SG`, `JP`, `HK` 等 |
| `githubPreferredURL` | GitHub 优选 IP 列表 URL | 默认已配置 |
| `enableGitHubPreferred` | 是否启用 GitHub 优选 | `true` / `false` |
| `enableOtherPreferred` | 是否启用域名优选 | `true` / `false` |
| `apiBaseUrl` | 订阅转换服务地址 | `https://url.v1.mk/sub` |

#### 第三步：生成 UUID（如需要）

```bash
# Linux/Mac
uuidgen

# Windows PowerShell
[guid]::NewGuid().ToString()

# 在线生成
# 访问 https://www.uuidgenerator.net/
```

#### 第四步：推送代码

```bash
# 添加所有修改
git add .

# 提交修改
git commit -m "更新配置"

# 推送到 GitHub（会自动触发混淆）
git push origin main
```

推送后，GitHub Actions 会自动：
1. 检测到 `明文源吗` 文件的修改
2. 运行混淆流程
3. 生成并提交 `少年你相信光吗` 文件

#### 第五步：部署到 Cloudflare

有三种部署方式：

##### 方式 1：Workers 部署（推荐）

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 进入 **Workers & Pages**
3. 点击 **Create Application** → **Create Worker**
4. 给 Worker 命名（例如：`my-proxy`）
5. 点击 **Deploy**
6. 部署成功后，点击 **Edit Code**
7. 复制 `少年你相信光吗` 的全部内容
8. 粘贴到代码编辑器中，替换默认代码
9. 点击 **Save and Deploy**

##### 方式 2：Pages 部署

1. 登录 Cloudflare Dashboard
2. 进入 **Workers & Pages**
3. 点击 **Create Application** → **Pages** → **Upload assets**
4. 从 GitHub Release 下载 `Pages.zip`
5. 上传 `Pages.zip` 文件
6. 完成部署

##### 方式 3：Snippets 部署

1. 复制 `snippets` 文件内容
2. 在 Cloudflare Dashboard 中使用 Snippets 功能
3. 粘贴代码并保存

#### 第六步（可选但推荐）：配置 KV 存储空间

KV 存储可以让你通过图形化界面管理配置，无需修改代码。**强烈推荐配置！**

##### 为什么需要 KV 存储？

- ✅ **图形化管理** - 通过 Web 界面管理优选 IP、协议配置等
- ✅ **无需重新部署** - 修改配置立即生效
- ✅ **动态更新** - 支持 API 管理优选列表
- ✅ **配置持久化** - 配置永久保存在 Cloudflare

##### Workers 中配置 KV

**步骤 1：创建 KV 命名空间**

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 进入 **Workers & Pages**
3. 点击左侧菜单的 **KV**
4. 点击 **Create a namespace** 按钮
5. 输入命名空间名称（例如：`proxy-config`）
6. 点击 **Add** 创建

**步骤 2：绑定 KV 到 Worker**

1. 返回 **Workers & Pages**
2. 点击你创建的 Worker（例如：`my-proxy`）
3. 进入 **Settings** 标签页
4. 找到 **Variables** 部分
5. 滚动到 **KV Namespace Bindings**
6. 点击 **Add binding** 按钮
7. 填写配置：
   - **Variable name**: `C` （必须是大写字母 C）
   - **KV namespace**: 选择刚才创建的 `proxy-config`
8. 点击 **Save** 保存
9. 点击 **Deploy** 重新部署 Worker

**步骤 3：验证 KV 配置**

1. 访问 `https://your-worker.workers.dev/{UUID}`
2. 如果看到图形化配置界面，说明 KV 配置成功
3. 你可以在页面上修改各种配置并保存

##### Pages 中配置 KV

**步骤 1：创建 KV 命名空间**（同上）

**步骤 2：绑定 KV 到 Pages**

1. 进入 **Workers & Pages**
2. 点击你的 Pages 项目
3. 进入 **Settings** 标签页
4. 找到 **Functions**
5. 滚动到 **KV namespace bindings**
6. 点击 **Add binding**
7. 配置：
   - **Variable name**: `C`
   - **KV namespace**: 选择 `proxy-config`
8. 点击 **Save** 保存

**步骤 3：重新部署**

1. 返回项目的 **Deployments** 页面
2. 点击最新部署右侧的 **︙** 菜单
3. 选择 **Retry deployment** 重新部署

##### KV 存储的配置项说明

通过图形化界面（访问 `/{UUID}`）可以配置：

| 配置项 | KV 键名 | 说明 |
|--------|---------|------|
| 优选 IP 列表 | `preferredIPs` | 自定义优选 IP 和端口 |
| 优选域名开关 | `enablePreferredDomain` | 启用/禁用域名优选 |
| 优选 IP 开关 | `enablePreferredIP` | 启用/禁用 IP 优选 |
| GitHub 优选开关 | `enableGitHubIP` | 启用/禁用 GitHub 优选 |
| VLESS 协议 | `enableVless` | 启用/禁用 VLESS |
| Trojan 协议 | `enableTrojan` | 启用/禁用 Trojan |
| xhttp 协议 | `enableXhttp` | 启用/禁用 xhttp |
| Trojan 密码 | `trojanPassword` | 自定义 Trojan 密码 |
| 自定义路径 | `customPath` | 修改访问路径 |
| API 管理开关 | `apiEnabled` | 启用/禁用 API 管理 |

##### 手动管理 KV 数据

如果需要手动编辑 KV 数据：

1. 进入 **Workers & Pages** → **KV**
2. 点击你的命名空间（`proxy-config`）
3. 点击 **Add entry** 添加新配置
4. 或点击已有配置进行编辑

**常用 KV 配置示例：**

```
键：preferredIPs
值：1.1.1.1:443#香港节点,8.8.8.8:443#美国节点

键：enableVless
值：true

键：enableTrojan
值：false

键：customPath
值：mypath
```

#### 第七步：访问订阅

部署完成后，访问以下地址：

```
# 图形化管理界面
https://your-worker.workers.dev/{你的UUID}

# 或使用自定义路径（如果设置了 customPath）
https://your-worker.workers.dev/{自定义路径}

# 订阅地址
https://your-worker.workers.dev/{你的UUID}/sub
```

---

## 📚 详细使用教程

### 如何修改代码

1. **编辑明文源码**
   ```bash
   # 打开文件
   nano 明文源吗
   # 或使用你喜欢的编辑器
   ```

2. **修改配置参数**
   - 修改 `authToken` 为你的 UUID
   - 根据需要调整其他配置

3. **推送更新**
   ```bash
   git add 明文源吗
   git commit -m "更新配置"
   git push
   ```

4. **等待自动混淆**
   - GitHub Actions 会自动运行
   - 查看 Actions 标签页确认执行状态
   - 混淆完成后会自动提交 `少年你相信光吗`

### 如何发布新版本

1. **创建标签**
   ```bash
   # 创建标签
   git tag v1.0.0
   
   # 推送标签
   git push origin v1.0.0
   ```

2. **自动发布**
   - GitHub Actions 会自动创建 Release
   - Release 中包含 `Pages.zip` 文件
   - 可直接用于 Cloudflare Pages 部署

### 订阅管理界面使用

访问 `https://your-worker.workers.dev/{UUID}` 后：

1. **选择客户端**
   - 点击对应客户端按钮
   - 支持的客户端会自动唤醒应用
   - 不支持的会复制订阅链接

2. **获取订阅链接**
   - 点击"获取订阅链接"按钮
   - 自动复制 Base64 编码的订阅内容

3. **查看相关链接**
   - GitHub 项目地址
   - YouTube 教程视频

### 优选 IP 配置

#### 方式 1：代码中配置

编辑 `directDomains` 数组：

```javascript
const directDomains = [
    { name: "香港节点", domain: "hk.example.com" },
    { name: "美国节点", domain: "us.example.com" },
    // 添加更多...
];
```

#### 方式 2：使用 GitHub 优选

修改 `githubPreferredURL`：

```javascript
const githubPreferredURL = 'https://raw.githubusercontent.com/your/repo/main/ips.txt';
```

IP 列表格式：
```
1.1.1.1:443#香港节点
8.8.8.8:443#美国节点
```

### 地区智能匹配

系统会根据 Worker 所在地区自动选择最优代理节点：

- **同地区优先** - 首选相同地区的节点
- **邻近地区次之** - 其次选择邻近地区
- **其他地区兜底** - 最后选择其他地区

手动指定地区：

```javascript
const manualWorkerRegion = 'SG';  // 新加坡
```

支持的地区代码：
- `US` - 美国
- `SG` - 新加坡  
- `JP` - 日本
- `HK` - 香港
- `KR` - 韩国
- `DE` - 德国
- `SE` - 瑞典
- `NL` - 荷兰
- `FI` - 芬兰
- `GB` - 英国

---

## 🔧 高级配置

### 自定义路径

不使用 UUID，改用自定义路径访问：

```javascript
const customPath = 'mypath';  // 或 'path/to/sub'
```

访问地址变为：
```
https://your-worker.workers.dev/mypath
https://your-worker.workers.dev/mypath/sub
```

### SOCKS5 代理

配置上游 SOCKS5 代理：

```javascript
const socks5Config = 'user:pass@host:port';
// 或不需要认证
const socks5Config = 'host:port';
```

### 备用地址

设置连接失败时的备用地址：

```javascript
let fallbackAddress = 'backup.example.com';
let fallbackPort = '443';
```

### 订阅转换服务

自定义订阅转换服务：

```javascript
const apiBaseUrl = 'https://your-converter.com/sub';
```

---

## 🛠️ 故障排查

### 问题 1：GitHub Actions 失败

**症状**：推送后 Actions 执行失败

**解决方案**：
1. 检查 Actions 日志查看错误信息
2. 确认 `明文源吗` 文件语法正确
3. 确保 Node.js 版本兼容（项目使用 18）

### 问题 2：订阅无法访问

**症状**：访问订阅地址返回 403 或 404

**解决方案**：
1. 检查 UUID 是否正确
2. 确认 Cloudflare Worker 已正确部署
3. 检查自定义路径配置是否正确

### 问题 3：混淆后代码无法运行

**症状**：部署混淆代码后 Worker 报错

**解决方案**：
1. 检查混淆选项是否过于激进
2. 尝试使用 `snippets` 文件（未混淆版本）
3. 在混淆配置中调整 `obfuscationOptions`

### 问题 4：订阅为空

**症状**：订阅链接返回空内容

**解决方案**：
1. 检查优选开关是否正确配置
2. 确认 GitHub 优选 URL 可访问
3. 查看 Worker 日志排查错误

---

## 📁 文件说明

| 文件名 | 作用 | 是否必需 |
|--------|------|----------|
| `明文源吗` | 未混淆的源代码 | ✅ 必需 |
| `少年你相信光吗` | 混淆后的代码（自动生成） | ✅ 必需 |
| `snippets` | Snippets 专用版本 | ⭕ 可选 |
| `.github/workflows/obfuscate.yml` | 代码混淆工作流 | ✅ 必需 |
| `.github/workflows/test.yml` | 发布部署工作流 | ⭕ 可选 |
| `README.md` | 项目文档 | ⭕ 可选 |

---

## 🔄 工作流程图

```
开发者修改代码
       ↓
   修改 明文源吗
       ↓
   git push
       ↓
 GitHub Actions 检测到变更
       ↓
 运行 obfuscate.yml
       ↓
   代码混淆处理
       ↓
 生成 少年你相信光吗
       ↓
 自动提交到仓库
       ↓
   创建 Tag
       ↓
  运行 test.yml
       ↓
 生成 Pages.zip
       ↓
 创建 GitHub Release
       ↓
 部署到 Cloudflare
```

---

## 🔐 安全建议

1. **保护 UUID**
   - 不要在公开场合分享你的 UUID
   - 定期更换 UUID 以提高安全性

2. **使用自定义路径**
   - 启用 `customPath` 提高隐蔽性
   - 避免使用容易猜测的路径名

3. **限制访问**
   - 考虑添加额外的认证机制
   - 使用 Cloudflare Access 保护敏感接口

4. **定期更新**
   - 关注项目更新
   - 及时修复安全漏洞

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

## 🙏 致谢

- 本项目基于 [zizifn/edgetunnel](https://github.com/zizifn/edgetunnel) 修改
- 感谢 Cloudflare 提供的 Workers 服务
- 感谢 JavaScript Obfuscator 项目

---

## 📞 联系方式

- **Issues**: [GitHub Issues](https://github.com/zhengwuji/cfnew-change/issues)
- **Discussions**: [GitHub Discussions](https://github.com/zhengwuji/cfnew-change/discussions)

---

<div align="center">

**如果这个项目对你有帮助，请给个 ⭐ Star！**

Made with ❤️ by [zhengwuji](https://github.com/zhengwuji)

</div>
